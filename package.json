{
  "name": "ai-smart-chatbot-backend",
  "version": "1.0.0",
  "description": "Backend API for AI Smart Chatbot with RAG system",
  "main": "backend/main.py",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 10000",
    "test": "pytest backend/tests/ -v"
  },
  "dependencies": {
    "fastapi": "^0.109.0",
    "uvicorn[standard]": "^0.27.0",
    "pydantic": "^2.5.3",
    "pydantic-settings": "^2.1.0",
    "python-multipart": "^0.0.6",
    "httpx": "^0.26.0",
    "chromadb": "^0.4.22",
    "openai": "^1.10.0",
    "beautifulsoup4": "^4.12.3",
    "python-dotenv": "^1.0.1"
  },
  "devDependencies": {
    "pytest": "^7.4.4",
    "pytest-asyncio": "^0.23.3"
  },
  "engines": {
    "python": "3.11"
  }
}